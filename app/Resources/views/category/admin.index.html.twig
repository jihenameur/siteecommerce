{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categories list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Descriptif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td><a href="{{ path('category_admin_show', { 'id': category.id }) }}">{{ category.id }}</a></td>
                <td>{{ category.libelle }}</td>
                <td>{{ category.descriptif }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('category_admin_show', { 'id': category.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('category_admin_edit', { 'id': category.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('category_admin_new') }}">Create a new category</a>
        </li>
    </ul>
{% endblock %}
