{% extends 'base.html.twig' %}

{% block body %}
    <h1>Products list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Price</th>
                <th>Descriptif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
                <td><a href="{{ path('product_admin_show', { 'id': product.id }) }}">{{ product.id }}</a></td>
                <td>{{ product.libelle }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.descriptif }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('product_admin_show', { 'id': product.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('product_admin_edit', { 'id': product.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('product_admin_new') }}">Create a new product</a>
        </li>
    </ul>
{% endblock %}
